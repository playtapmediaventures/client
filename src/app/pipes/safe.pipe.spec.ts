import { inject, TestBed } from '@angular/core/testing';
import {StyleSafe} from './safe.pipe';


describe('StyleSafe', () => {

  beforeEach(inject([], () => {
    this.pipe = new StyleSafe();
  }));

  xit('what does this pipe do?');

});
